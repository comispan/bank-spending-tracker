pdfplumber>=0.11
pypdf>=5.0
pypdfium2>=4.30   # page rendering for vision mode
pillow>=10.0      # PNG encoding for vision mode
anthropic>=0.40
openai>=1.60      # only needed for SPIKE_BASE_URL providers (Groq, DeepSeek, ...)
reportlab>=4.0    # only needed for make_sample.py
